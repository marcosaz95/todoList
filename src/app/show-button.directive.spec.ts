import { ShowButtonDirective } from './show-button.directive';

describe('ShowButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowButtonDirective();
    expect(directive).toBeTruthy();
  });
});
